<h1>Followings</h1>

<ul>
  <% @followings.each do |following| %>
    <li>
      <%= link_to following.name, user_path(following) %>  |
      <%= link_to "UnFollow", follow_path( following.id), method: :delete, data: { confirm: "Are you sure?" }, class:"btn" %>
    </li>
  <% end %>
    </ul>